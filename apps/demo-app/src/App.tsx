import React from "react";
import "./App.css";
// import { Button } from "@sigil/components-react"; // Uncomment when components are available

export default function App() {
  return (
    <main>
      <h1>Sigil Demo App</h1>
      <p>
        This app consumes the design system and tokens via workspace packages.
      </p>

      {/* Example of a component from the design system */}
      {/* <Button variant="primary" onClick={() => alert("Clicked!")}>
        Click Me
      </Button> */}
    </main>
  );
}
